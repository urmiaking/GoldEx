@using Microsoft.JSInterop
@inherits LayoutComponentBase
@layout BaseLayout

<MudLayout>
    <AppBar @bind-DrawerOpen="_drawerOpen" />
    <Drawer @bind-IsDrawerOpen="_drawerOpen" BreakpointChanged="() => StateHasChanged()" />
    <MudMainContent Class="mt-10 mb-sm-4 mb-20 pa-md-4 py-4">
        @Body

        <MobileNav Class="d-sm-none d-block" />
    </MudMainContent>
</MudLayout>

@code
{
    private bool _drawerOpen;
    [Inject] private IJSRuntime JsRuntime { get; set; } = default!;


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JsRuntime.InvokeVoidAsync("removeLoadingIndicator");

        await base.OnAfterRenderAsync(firstRender);
    }
}
