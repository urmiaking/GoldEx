@attribute [Route(ClientRoutes.InventoryStocks.List)]
@attribute [Authorize(Roles = $"{BuiltinRoles.Administrators}, {BuiltinRoles.Owners}")]
@inherits GoldExComponentBase

<PageTitle>لیست موجودی</PageTitle>

<MudContainer MaxWidth="MaxWidth.ExtraLarge">
    <MudBreadcrumbs Class="pr-0" Items="_breadcrumbs" MaxItems="2">
        <SeparatorTemplate>
            <MudIcon Icon="@Icons.Material.Filled.ArrowForwardIos" Size="Size.Small" />
        </SeparatorTemplate>
    </MudBreadcrumbs>
    
    <MudText Typo="Typo.h5" GutterBottom="true" Class="text-center">لیست موجودی انبار</MudText>
    <MudDivider Class="mb-2" />

    <InventoryStockList Class="mb-4" ContainerClass="responsive-table-toolbar" SearchQuery="@SearchQuery" />

</MudContainer>