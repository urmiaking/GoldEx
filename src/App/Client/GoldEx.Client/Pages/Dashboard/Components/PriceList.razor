@inherits GoldExComponentBase

<MudText Typo="Typo.h5" GutterBottom="true">نرخ لحظه ای</MudText>
<MudDivider Class="mb-2" />

@if (IsBusy)
{
    <MudGrid>
        @for (var i = 0; i < 8; i++)
        {
            <MudItem xs="12" sm="6" md="3">
                <PriceCardSkeleton />
            </MudItem>
        }
    </MudGrid>
}
else
{
    <MudGrid>
        @if (!_items.Any())
        {
            <MudItem xs="12">
                <MudAlert Severity="Severity.Info">اطلاعاتی برای نمایش وجود ندارد.</MudAlert>
            </MudItem>
        }
        else
        {
            foreach (var item in _items)
            {
                <MudItem xs="12" sm="6" md="3">
                    <PriceCard Title="@item.Title"
                               ImageUrl="@(ApiUrls.Icons.Get(IconType.Price, item.Id))"
                               ChangePercent="@ExtractPercentChange(item.Change)"
                               Price="@item.Value"
                               PriceUnit="@item.Unit"
                               DateTime="@item.LastUpdate" />
                </MudItem>
            }
        }

    </MudGrid>
}
