@using GoldEx.Shared.Enums
@inherits GoldExComponentBase

@*
    File: PriceCard.razor
    Purpose: A reusable component to display a single price card with smooth real-time flash indicators.
*@

<MudCard Elevation="2" Outlined="true" Class="@($"rounded-lg price-card-base {_flashClass}")">
    <MudCardContent Class="price-card-content">
        <MudStack Spacing="2">
            <!-- Top Row: Percentage Change and Title/Icon -->
            <div class="d-flex justify-space-between align-center">
                
                <!-- Title and Image -->
                <div class="align-center d-flex flex-row-reverse gap-2">
                    <MudText Typo="Typo.h6" Class="mr-2">@Title</MudText>
                    <MudAvatar Size="Size.Medium">
                        <MudImage Src="@ImageUrl" Alt="@Title" />
                    </MudAvatar>
                </div>

                <!-- Percentage Change Chip -->
                <MudChip Icon="@ChangeIcon" 
                         Color="@ChangeColor" 
                         Size="Size.Medium" 
                         Variant="Variant.Text" 
                         T="string"
                         Label="true">
                    @($"{Math.Abs(ChangePercent):F2}%")
                </MudChip>
            </div>

            <!-- Bottom Row: Price and DateTime -->
            <div class="d-flex justify-space-between align-center mt-2">
                <!-- DateTime Stamp -->
                <MudText Typo="Typo.caption">@DateTime?.ToString("yyyy/MM/dd HH:mm:ss")</MudText>
                
                <!-- Price and Unit -->
                <div class="d-flex align-baseline gap-1">
                    <MudText Typo="Typo.h5" Class="font-weight-bold price-val-highlight">@Price</MudText>
                    <MudText Typo="Typo.body2" Class="ml-1">@PriceUnit</MudText>
                </div>
            </div>
        </MudStack>
    </MudCardContent>
</MudCard>
