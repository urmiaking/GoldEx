@attribute [Route(ClientRoutes.Invoices.List)]
@attribute [Authorize(Roles = $"{BuiltinRoles.Administrators}, {BuiltinRoles.Owners}")]
@inherits GoldExComponentBase

<PageTitle>فاکتورها</PageTitle>

<MudContainer MaxWidth="MaxWidth.ExtraLarge">
    <MudBreadcrumbs Class="px-0" Items="_breadcrumbs" MaxItems="2">
        <SeparatorTemplate>
            <MudIcon Icon="@Icons.Material.Filled.ArrowForwardIos" Size="Size.Small" />
        </SeparatorTemplate>
    </MudBreadcrumbs>
    
    <MudText Typo="Typo.h5" GutterBottom="true" Class="text-center d-md-block d-none">لیست فاکتورها</MudText>
    <MudDivider Class="mb-2 d-md-block d-none" />

    <InvoicesList ContainerClass="responsive-table-toolbar" PersistState="true" />

</MudContainer>